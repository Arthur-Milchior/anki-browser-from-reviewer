{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Reviewer to Browser choosing what to display",
  "properties": {
    "open": {
      "type": "string",
      "default": "note",
      "enum": [
        "note",
        "card",
        "deck"
      ]
    }
  }
}
